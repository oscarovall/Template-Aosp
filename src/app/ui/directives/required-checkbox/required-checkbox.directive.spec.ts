import { RequiredCheckboxDirective } from './required-checkbox.directive';

describe('RequiredCheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
