import { DateagoPipePipe } from './dateago-pipe.pipe';

describe('DateagoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateagoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
