import { ClassGroupPipe } from './class-group.pipe';

describe('ClassGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
