import { HightlightPipe } from './hightlight.pipe';

describe('HightlightPipe', () => {
  it('create an instance', () => {
    const pipe = new HightlightPipe();
    expect(pipe).toBeTruthy();
  });
});
