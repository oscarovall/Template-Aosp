import { ClasscolPipePipe } from './classcol-pipe.pipe';

describe('ClasscolPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ClasscolPipePipe();
    expect(pipe).toBeTruthy();
  });
});
