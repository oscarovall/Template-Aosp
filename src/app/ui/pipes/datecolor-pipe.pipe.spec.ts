import { DatecolorPipePipe } from './datecolor-pipe.pipe';

describe('DatecolorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DatecolorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
